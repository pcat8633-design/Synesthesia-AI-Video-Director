Develop a plan for the following:

Check how resolution downgrades are currently being handled.  Looks like 5-second shots are being incorrectly down-graded to 720p (probably a rounding error).

Also, we need to take another look at "vocal Chain last frame" on tab 3.  This should be separated out from first frame mode so the user can still choose LTX-native plus "Chain last frame" OR Z-image first frame plus "Chain last frame."  In these instances obviously only the first frame of the series of vocal shots would be effected by the selection of Z vs LTX and the remainder in that series would get the last frame of the previous shot.   

Also think about any gotcha's or inconsistencies with this plan and develop fixes before we start.

